# 新会话开发指令

将下面这段发给新会话，并附本目录SPEC.md。该指令启动开发；当前会话只生成文档。

---

请根据 `/Users/sjl/Downloads/社招简历/公司面试准备/Elekta/ClinicFlow/SPEC.md` 开发ClinicFlow，先读它链接的四份spec，然后检查目录与当前Git状态。方案已完成讨论，不要重新开展需求访谈。

我熟练Java，希望通过这个项目学习C#/.NET并准备Elekta面试。请先搭出可运行的ASP.NET Core＋React/TypeScript＋MySQL系统，同时提供代码阅读、Java对照、系统设计、故障实验和面试文档。我之后会阅读、修改并完成独立需求；初版保留该练习不要代做。

按 `spec/03-开发与Git交付.md` 的M0–M8逐功能实施。每完成一个功能，运行相关检查，更新文档，独立commit并push到我的GitHub仓库，报告提交和测试结果，再继续下一功能。不要把全部代码积累成最后一次大提交。目标确定后的每次普通commit/push已获授权，无需重复询问。

首先核实SDK与数据库驱动版本、Docker与启动方式。检查已有远程；如果没有仓库地址，请向我索取URL或新建仓库所需的名称/归属/可见性，同时继续不依赖远程的本地工作。只提交ClinicFlow项目内容，不上传上级简历和面试资料。不要替我猜公开仓库或远程分支。

对照验收ID逐项保留实际测试证据，保证并发、改期回滚、幂等和Outbox故障可复现。实现细节可以按证据调整并记录ADR；保留已确认的功能和验收目标。已有进度则从未完成切片继续，不重置用户改动。

---

## 启动时由用户补充

- GitHub仓库URL：尚未提供。
- 若新建仓库：仓库名称、所属账号/组织、公开或私有。
- 目标分支：沿用明确的仓库约定，或由用户指定。

除远程交付所需信息外，其余已确认设计见SPEC；无需再次确认技术栈、业务范围和逐功能push方式。
